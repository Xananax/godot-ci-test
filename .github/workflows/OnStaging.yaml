name: "Staging->GH-Pages"

on:
  push:
    branches: [staging]
  workflow_dispatch:

jobs:
  build-staging:
    name: build and push to gh-pages
    uses: ./.github/workflows/BuildGodot@v1
    with:
      sub_build_path: staging
    secrets:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}